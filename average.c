#include<stdio.h>
int main(void)
{
	int n,i,score,count=0,s=0;
	float average;
	scanf("%d",&n);
	scanf("%d",$score);
	for(i=1;i<=n;i++)
	{
		s=s+score;
		if(score>=60)
			count++;
		scanf("%d",$score);
	}
	average=s*1.0/n;
	printf("average=%.1f",averge);
	printf("count=%d",count);
}